Next.js 16 image upload: why writing files to public/ works in dev but 404s in production (not in the build manifest), plus the secure write+serve-via-API fix — POST /api/upload, GET /api/.../images/filename, path-traversal & MIME hardening, immutable cache, next/image with localPatterns/unoptimized, serverless → object storage.

Steps: 
- Read The document
- Understand the concept
- Download the file
- And give this to your AI assistant and say: `proceed`
